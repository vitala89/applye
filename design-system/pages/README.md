# Page-specific design overrides

One file per page/screen, named after it (e.g. `profile.md`, `dashboard.md`).

Record **only what deviates** from `../MASTER.md` - do not restate the global system.
If a design reference (link or mock) is given for a screen, capture its deltas here so
the next agent inherits them instead of re-drifting.
